import Controller from '@ember/controller';

export default Controller.extend({
    // actions: {
    //     readAPI() {
    //         let id = this.get('id')
    //         this.store.findAll('id').then(data => {
    //             console.log(data.get('id'))
    //         })
    //     }
    // }
});
